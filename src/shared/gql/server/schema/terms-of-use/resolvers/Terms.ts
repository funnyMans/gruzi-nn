import type { TermsResolvers } from './../../types.generated';
export const Terms: TermsResolvers = {
  general: (terms) => terms.general,
  sections: (terms) => terms.sections,
  acknowledgement: (terms) => terms.acknowledgement,
  /* Implement Terms resolver logic here */
};
